export interface ProfileDataInterface {
  email: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  verified: boolean;
}

export interface PostProfileInterface {
  firstName: string;
  lastName: string;
  avatar: string;
  phoneNumber: string;
}
